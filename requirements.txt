# E-Commerce Intelligence Platform — pipeline + ML dependencies
# Python 3.12.x required (pin via .python-version)
# Install: pip install -r requirements.txt

# --- Data layer ---
duckdb==0.10.3
pandas==2.2.2
numpy==1.26.4
pyarrow==16.1.0

# --- dbt ---
dbt-core==1.8.3
dbt-duckdb==1.8.1

# --- Data quality ---
great-expectations==0.18.19

# --- ML ---
scikit-learn==1.5.0
xgboost==2.0.3
prophet==1.1.5
torch==2.3.1          # CPU only — set USE_TF=0 USE_FLAX=0 before import
transformers==4.41.2
mlflow==2.13.2
joblib==1.4.2
shap==0.45.1
mlxtend==0.23.1       # Apriori

# --- API ---
fastapi==0.111.0
uvicorn[standard]==0.30.1
pydantic==2.7.3

# --- Storage / cloud ---
supabase==2.4.6

# --- BigQuery / GCP ---
google-cloud-bigquery==3.22.0
google-cloud-bigquery-storage==2.25.0   # fast Arrow reads for Render API
dbt-bigquery==1.8.2
db-dtypes==1.2.0                        # BQ → pandas type bridge
google-auth==2.29.0

# --- Utils ---
python-dotenv==1.0.1
kaggle==1.6.14
requests==2.32.3
tqdm==4.66.4
