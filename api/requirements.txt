# Dependencies for the RetailLens API service (deployed on Render).
# Kept minimal: serving only loads pre-trained artifacts and runs inference.
# Heavy training-only libraries (scipy, scikit-learn, implicit) are NOT needed
# here - training happens offline on the developer machine / CI.

fastapi==0.115.6
uvicorn[standard]==0.34.0
sqlalchemy==2.0.36
psycopg2-binary==2.9.10
python-dotenv==1.0.1

# ML serving: load joblib artifacts and run inference.
# xgboost is required to unpickle the reactivation model; pandas builds the
# single-row inference frame; supabase downloads artifacts from Storage.
joblib==1.4.2
xgboost==2.1.3
pandas==2.2.3
supabase==2.11.0
pydantic==2.10.4
